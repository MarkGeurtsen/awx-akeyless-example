---
- name: Akeyless AWX Example
  hosts: localhost
  gather_facts: no
  connection: local

  tasks:
    - name: Show Akeyless-injected credentials (env)
      ansible.builtin.debug:
        msg: "Secret username is {{ lookup('env','USERNAME') }} and password is {{ lookup('env','PASSWORD') }}"
